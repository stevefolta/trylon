# Trylon settings.

program = "bench"

iff !in-trylid
	# Trylon boilerplate.
	c-flags += "-g"
	link-flags += "-lgc" "-ldl" "-lm"

	if Darwin
		c-flags += "-DOSX_FINK" "-I/sw/include"
		link-flags += "-L/sw/lib"


