# Trylon settings.

program = "trylid"
debugging = true

iff !in-trylid
	c-flags += "-g"
	link-flags += "-lgc" "-ldl" "-lm"

	if Darwin
		c-flags += "-DOSX_FINK" "-I/sw/include"
		link-flags += "-L/sw/lib"



# Trylid settings.

iff in-jolt
	jolt-sources-dir = ".jolt-sources-self"

