trylon TrylonFunction


emit-call: arguments builder: builder
	virtual

emit-declaration: stream
	virtual

prepare-to-emit
	# Default: nothing to do.

emit-code: stream
	# Default: nothing to do.

used-by-lambda: block
	# Default: most TrylonFunctions don't care.

has-code
	return true


# Most TrylonFunctions are non-object functions, and object functions and
# non-object functions are mostly disjoint sets, but the exception is
# CompiledClass.  So a TrylonFunction can represent either type of function.

# The rest of these must be defined for object functions.

emit-object-call: object arguments: arguments builder: builder
	virtual

c-name
	virtual

is-overridden
	# Default: no.
	return false




# Copyright 2005-2007 Steve Folta
# See the License file.

