trylon Temporary

fields name


create: index
	name = "t" + index string + "_"

emit-declaration: stream
	stream write-all: ("obj_ ", name, ";\n")

access
	return name


index: new-index
	# Don't do anything; we already have our index, but an IndexedResources will
	# try to set our index when we're added to it.

