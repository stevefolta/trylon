trylon LocalResult
superclass Result

fields offset

create: local
	offset = -(local compile-info index + 1) * word-size


mod-rm
	if offset < -127
		return 0xB5
	else
		return 0x75

disp8
	if offset >= -127
		return offset
	return nil

disp32
	if offset < -127
		return offset
	return nil


