trylon ArgumentResult
superclass Result

fields offset

create: index
	offset = (argument compile-info index + 2) * word-size


mod-rm
	if offset > 127
		return 0xB5
	else
		return 0x75

disp8
	if offset < 127
		return offset
	return nil

disp32
	if offset > 127
		return offset
	return nil



