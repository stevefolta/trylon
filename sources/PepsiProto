trylon PepsiProto
superclass CompiledProto

create: name parent: parent
	super create: name parent: parent


get-subproto: name
	# *Always* return it.
	# Just return a CompiledProto, without calling add-proto, so we don't try to
	# do any codegen for it.
	return CompiledProto new: name parent: this



