[2007.8.31]

	trylon main

	fields my-field

Classes can see "my-field" (which is correct), but the codegen is a field access, when it should be a send to the "Main" proto.


=====

To Do
	[live]

Fix context field bug (see 2007.8.31).
Lambdas.
Field access optimization:
	Explicit calls on "this" (as in constructors).


